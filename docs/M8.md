# M8 — Projects and agents

First-class projects that sync through the vault, one-click open → cd → launch
agent, and persistent remote sessions via `tmux`. Detail and rationale live in
[`ROADMAP-v2.md`](../ROADMAP-v2.md).

## Goals

1. **`Project`** — Local `{ path }` or Remote `{ host_id, path }`; syncs like Host.
2. **`AgentSpec`** — built-in presets (data only) + optional custom later.
3. **Open project** — connect/reuse → `cd` → launch default (or chosen) agent.
4. **Persistence** — remote + `persistent: true` → `tmux new-session -A …`;
   zellij fallback; plain shell with a clear warning.
5. **Running sessions** — list/reattach (cross-device via vault sync).

## Build order

| Slice | Scope | Status |
|---|---|---|
| 1 | Model + vault CRUD + sync + IPC list/create/update/delete | Done |
| 2 | Presets + UI (sidebar/palette) + open → cd → launch | Done |
| 3 | tmux wrap + non-persistent indicator | Done (tmux → zellij → warn) |
| 4 | Running-sessions view / reattach | Done |

## Running sessions

Opening a remote project with a persistent agent upserts a vault
`RunningSession` (one per project). It syncs like hosts/projects, so another
device can show it under **Running** and reattach with the same
`tmux new-session -A` path. Clearing a row only drops the vault marker — it
does not kill the remote tmux session.

## Verify

```bash
cargo test -p core --lib running_session
cargo test -p tethra export_bindings
npm run build --prefix apps/ui
```
